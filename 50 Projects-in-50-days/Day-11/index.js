// Declare 2 variables x & y that are Numbers

let x = 10;
let y = 100;
const solution = `The sum ${x} + ${y} is ${x + y}`

// Declare a string 'solution' that when printed to the console reads like:
// "The sum of <x> and <y> is <x + y>"
// ADD CODE BELOW
 
console.log(solution)


